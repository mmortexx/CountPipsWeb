/* Tamaño de la muestra de la demo, aparte del generador: el cierre de las
   fichas del glosario lo cita, y es un componente de cliente que sale en
   decenas de páginas. Importar `TRADES` para contarlas metería las 200
   operaciones en el JS de todas ellas. */
export const OPERACIONES_MUESTRA = 200;
